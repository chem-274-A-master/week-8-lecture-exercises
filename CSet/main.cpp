#include <iostream>
#include <vector>
#include <set>

/* Using a set, find the unique values of a vector
 *
 * Then, print out the unique values (one per line)
 *
 * Remember the proper header!
 */


int main() {

  std::vector<int> vec = {1, 7, -1, 9, 7, 3, 8, -7, 9, -9, -8, 7, 3, 1, 2, -1, 2, 1, -8, 7, 3, -6, -5, -1, 3};

  /* 1. Find unique values using a set */

  /* 2. Print the elements of the set, one per line */
}