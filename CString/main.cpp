#include <iostream>
#include <vector>

/* Write a function that, given an std::vector<string>, returns a string where
   each element of the vector is on its own line.

   HINT: You cannot append std::endl to a string. Instead, use "\n"
*/


int main() {

  std::vector<std::string> vec = {
    "Hello",
    "World!",
    "I am but",
    "a simple vector of strings."
  };

  // Create a string using your function and the above vector, then
  // print to the standard output with std::cout
}